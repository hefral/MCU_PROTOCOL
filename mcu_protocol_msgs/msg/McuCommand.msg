# 下行命令（MCU 帧类型 0x10，桥接节点按 20 Hz 发出，整帧 40 B）。
#
# 8 个 float 的语义由 HELLO_ACK 里的 cmd_layout 字节声明，具体映射表属于契约 §6
# 待定项，因此这里保持原始数组。桥接节点校验长度必须精确为 8：MCU 对命令帧做
# 严格长度校验，多一字节少一字节都直接拒收，不会部分解释。
#
# 发布频率不必是 20 Hz。桥接节点做零阶保持 —— 只记住最新一条，由自己的 20 Hz
# 定时器发往 MCU。上游用 5 Hz 还是 100 Hz 都可以，线上始终是 MCU 期望的 20 Hz。
#
# 但停发是有含义的：超过 cmd_timeout_ms（默认 150 ms）没有新消息，桥接节点会
# 停止发送命令帧，让 MCU 的看门狗按 200 ms 降级 / 500 ms 输出全零走。不要为了
# 「保持链路」而空转发布 —— 那会绕过两端的看门狗。

float32[8] values
